PRODUCTOS
-ID del producto (automático - Integer)
-Nombre del producto (String)
-Categoría del producto (Drop liste clase cateogoría)
-Cantidad en stock (int)
-Precio venta (BigDecimal)


CLIENTES
-Cédula (Integer)
-Nombre y apellidos (String)
-Correo (String - expresión regular - opcional)
-Teléfono (String - opcional)
-Compras registradas (HashMap <Integer, Venta>, no se muestra en el formulario solo en la tabla)


PROVEEDORES
-Nit (Integer)
-Nombre del proveedor (String)
-Correo (String - expresión regular)
-Teléfono (String)
-Compra registradas (HashMap <Integer, Compra>)


VENTA (A CLIENTES)
-ID de la venta  (automático - Integer)
-Fecha de la venta (automático por defecto con posibilidad de editar - LocalDate)
-Cédula del cliente (Integer)
-Nombre y apellidos (String - automático)
-Lista de productos vendiddos (HashMap <Integer idPrdoucto, Integer cantidad>) # Pensar en visualización y edición de los productos añadidos
-Costo de la venta (BigDecimal)


COMPRA (A PROOVEDORES)
-ID de la compra (automático - Integer)
-Fecha de la compra (automático por defecto con posibilidad de editar - LocalDate)
-Nit del proveedor (Integer)
-Nombre del proveedor (String - automático) 
-Lista de productos comprados (HashMap <Integer idPrdoucto, Integer cantidad>) # Pensar en visualización y edición de los productos añadidos
-Campo adicional en la GUI para el precio de cada producto; lista de precios (HashMap<Integer idProducto, BigDecimal precioProducto>)
-Costo de la compra (BigDecimal)


Stock #No se necesita, se en realidad listar el stock es listar los productos, que ya se está haciendo la categoría PRODUCTOS.
Drop para filtrar por categoría del producto 